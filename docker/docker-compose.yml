services:
  portfolio:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - '${HOST_BIND:-127.0.0.1}:${HOST_PORT:-8080}:8080'
    expose:
      - '8080'
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    name: web
